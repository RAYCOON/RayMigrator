{
  "description": "Repository type common to all (base), repository connection per environment (environment files), error action per product (product files), target connection per combination (product-environment files); the base holds every product with all it shares"
}
